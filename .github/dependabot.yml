# Keeps the SHA-pinned actions in .github/workflows/*.yml current: Dependabot
# bumps the commit hash and the trailing version comment together.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
